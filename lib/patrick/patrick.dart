import 'package:flutter/material.dart';

class Patrick extends StatelessWidget {
  const Patrick({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      width: 100,
      color: Colors.white24,
    );
  }
}
